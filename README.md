# RateMaster
RateMaster is an API Gateway that sits between your clients and backend microservices — managing traffic flow, keeping an eye on what's coming in, and slowing things down when needed. It supports multiple rate limiting algorithms, so you can swap in whatever fits your use case.
